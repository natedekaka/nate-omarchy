#!/bin/bash

# omarchy:summary=Launch the fastfetch TUI that gives information about the current system.

exec foot --window-size-chars=110x30 --app-id=org.omarchy.fastfetch -e bash -c 'fastfetch; read -n 1 -s'
